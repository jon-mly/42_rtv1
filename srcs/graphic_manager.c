#include "rtv1.h"

void		calculate_scene(t_env *env)
{
	int			x;
	int			y;

	x = -1;
	while (++x < env->win_width)
	{
		y = -1;
		while (++y < env->win_height)
		{
			// TODO:
			// - trace ray 
			// - get color
			// apply color to pixel
		}
	}
}
